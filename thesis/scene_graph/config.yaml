input_path: "data/output/Replica/small_test_orb_exported/"
output_folder: "data/output/small_test_orb_geometry/"

# Minimum horizontal intersection-over-minimum required for above/below/on relations
spatial_graph:
  vertical_iom: 0.5
  near_thres: 0.01
  on_thres: 0.01
  under_thres: 0.01
  relations:
    above: True
    below: True
    near: True
  
fusion:
  distance_scale: 
  distance_threshold:
  point_distance_threshold:
  semantic_threshold:
  coverage_threshold:
k_top_views: 10
